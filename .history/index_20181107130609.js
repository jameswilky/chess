function StopWatch() {
  let startTime,
    endTime,
    running,
    duration = 0;
  this.start = function() {};
  this.stop = function() {};
  this.reset = function() {
    let x = 0;
  };
}

const sw = new StopWatch();
